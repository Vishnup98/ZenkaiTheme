{%- assign current_variant = product.selected_or_first_available_variant -%}
{%- assign hero_media = product.media.first -%}
{%- assign lifestyle_media = product.media[1] | default: hero_media -%}
{%- assign hand_media = product.media[2] | default: lifestyle_media -%}

{{ 'evolution-pin-set.css' | asset_url | stylesheet_tag }}

<div
  id="ProductSection-{{ product.id }}"
  class="evo-pin-page"
  data-evolution-pin-page
  data-section-id="{{ product.id }}"
  data-section-type="product-template"
  data-enable-history-state="true"
  data-variant-type="button"
  data-product-category="pin">
  <div class="evo-pin-page__ambient evo-pin-page__ambient--one" aria-hidden="true"></div>
  <div class="evo-pin-page__ambient evo-pin-page__ambient--two" aria-hidden="true"></div>

  <div class="evo-pin-shell">
    <nav class="evo-pin-breadcrumb" aria-label="Breadcrumb">
      <a href="{{ routes.root_url }}">Home</a>
      <span aria-hidden="true">/</span>
      <a href="{{ routes.all_products_collection_url }}">All products</a>
      <span aria-hidden="true">/</span>
      <span aria-current="page">{{ product.title }}</span>
    </nav>

    <section class="evo-pin-hero" aria-labelledby="evo-pin-title-{{ section.id }}">
      <div class="evo-pin-gallery" data-evo-gallery>
        <div class="evo-pin-gallery__stage">
          {%- if hero_media != blank -%}
            <img
              class="evo-pin-gallery__main"
              src="{{ hero_media.preview_image | image_url: width: 1600 }}"
              srcset="{{ hero_media.preview_image | image_url: width: 720 }} 720w, {{ hero_media.preview_image | image_url: width: 1100 }} 1100w, {{ hero_media.preview_image | image_url: width: 1600 }} 1600w"
              sizes="(min-width: 990px) 56vw, 100vw"
              width="{{ hero_media.preview_image.width }}"
              height="{{ hero_media.preview_image.height }}"
              alt="{{ hero_media.alt | default: product.title | escape }}"
              loading="eager"
              fetchpriority="high"
              data-evo-main-image>
          {%- else -%}
            {{ 'product-1' | placeholder_svg_tag: 'evo-pin-gallery__placeholder' }}
          {%- endif -%}
          <div class="evo-pin-gallery__badge" aria-label="Complete collection">
            <span>8</span>
            <small>pins included</small>
          </div>
        </div>

        {%- if product.media.size > 1 -%}
          <div class="evo-pin-gallery__thumbs" aria-label="Product gallery">
            {%- for media in product.media limit: 6 -%}
              {%- if media.media_type == 'image' -%}
                <button
                  class="evo-pin-gallery__thumb{% if forloop.first %} is-active{% endif %}"
                  type="button"
                  aria-label="Show product image {{ forloop.index }}"
                  aria-current="{% if forloop.first %}true{% else %}false{% endif %}"
                  data-evo-thumb
                  data-full-src="{{ media.preview_image | image_url: width: 1600 }}"
                  data-full-srcset="{{ media.preview_image | image_url: width: 720 }} 720w, {{ media.preview_image | image_url: width: 1100 }} 1100w, {{ media.preview_image | image_url: width: 1600 }} 1600w"
                  data-alt="{{ media.alt | default: product.title | escape }}">
                  <img
                    src="{{ media.preview_image | image_url: width: 240 }}"
                    width="{{ media.preview_image.width }}"
                    height="{{ media.preview_image.height }}"
                    alt=""
                    loading="lazy">
                </button>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>

      <div class="evo-pin-offer">
        <p class="evo-pin-eyebrow" data-evo-hero-eyebrow>The complete eight</p>
        <h1 id="evo-pin-title-{{ section.id }}" data-evo-hero-title>{{ product.title }}</h1>
        <p class="evo-pin-offer__lede" data-evo-hero-lede>Eight tiny forms. One complete little lineup. Made for the bag, jacket, board, or desk that still needs one more detail.</p>

        <div class="evo-pin-price" data-price-wrapper>
          {%- if current_variant.compare_at_price > current_variant.price -%}
            <s data-product-price-compare>{{ current_variant.compare_at_price | money }}</s>
          {%- endif -%}
          <span data-product-price>{{ current_variant.price | money }}</span>
        </div>

        <div class="evo-pin-trust" aria-label="Order highlights">
          <span><b aria-hidden="true">✓</b> All 8 included</span>
          <span><b aria-hidden="true">✓</b> U.S. delivery available</span>
          <span><b aria-hidden="true">✓</b> Secure checkout</span>
        </div>

        <div class="evo-pin-option" aria-label="Selected option">
          <div>
            <span class="evo-pin-option__label">Your set</span>
            <strong>8-Pin Collection</strong>
          </div>
          <span class="evo-pin-option__status">Complete set</span>
          <p>No single pins. No mystery assortment. The full color lineup arrives together.</p>
        </div>

        {%- form 'product', product, id: 'EvolutionPinForm', class: 'product-single__form evo-pin-form' -%}
          <input type="hidden" name="properties[_Landing angle]" value="" data-evo-angle-input>
          <select name="id" class="no-js" data-product-select aria-label="Product variant">
            {%- for variant in product.variants -%}
              <option value="{{ variant.id }}"{% if variant == current_variant %} selected{% endif %}{% unless variant.available %} disabled{% endunless %}>{{ variant.title }}</option>
            {%- endfor -%}
          </select>
          <button
            class="btn btn--full evo-pin-form__submit add-to-cart zenkai-primary-cta"
            type="submit"
            name="add"
            data-add-to-cart
            data-cta-label="Add 8-Pin Collection"
            data-cta-price="true"
            aria-live="polite"
            {% unless current_variant.available %}disabled{% endunless %}>
            <span data-add-to-cart-text>
              {%- if current_variant.available -%}
                Add 8-Pin Collection - {{ current_variant.price | money }}
              {%- else -%}
                Sold out
              {%- endif -%}
            </span>
          </button>
          <div class="hide" aria-hidden="true" data-variant-json>{{ product.variants | json }}</div>
        {%- endform -%}

        <p class="evo-pin-gift-note" data-evo-gift-note><strong>Easy gift. Zero size guessing.</strong> One complete collector set, ready to split across a few favorite places—or keep together.</p>

        <div class="evo-pin-mini-proof">
          <span aria-hidden="true">✦</span>
          <p data-evo-mini-proof><strong>The lineup is the point.</strong> Each color has its own mood; all eight make the set feel finished.</p>
        </div>
      </div>
    </section>
  </div>

  <section class="evo-pin-manifesto" aria-labelledby="evo-pin-manifesto-{{ section.id }}">
    <div class="evo-pin-shell evo-pin-manifesto__grid">
      <div>
        <p class="evo-pin-eyebrow" data-evo-manifesto-eyebrow>No choosing favorites</p>
        <h2 id="evo-pin-manifesto-{{ section.id }}" data-evo-manifesto-title>All eight, or it is not the collection.</h2>
      </div>
      <p data-evo-manifesto-copy>Built as one deliberate set: eight glossy enamel pins, eight distinct color stories, and one satisfying arrangement from the moment you open it.</p>
    </div>
    <div class="evo-pin-shell evo-pin-proof-grid" aria-label="Collection details">
      <article data-evo-proof-card>
        <span>01</span>
        <h3>Complete by design</h3>
        <p>Every pin shown is included. No random pull, duplicate, or separate add-on required.</p>
      </article>
      <article data-evo-proof-card>
        <span>02</span>
        <h3>Small, glossy, graphic</h3>
        <p>Bright enamel color and raised dark outlines keep the details readable up close and at a glance.</p>
      </article>
      <article data-evo-proof-card>
        <span>03</span>
        <h3>Made to move around</h3>
        <p>Keep the set together or spread the eight across bags, jackets, hats, and display boards.</p>
      </article>
    </div>
  </section>

  <section class="evo-pin-story evo-pin-story--dark" aria-labelledby="evo-pin-story-one-{{ section.id }}">
    <div class="evo-pin-shell evo-pin-story__grid">
      <div class="evo-pin-story__image">
        {%- if lifestyle_media != blank -%}
          <img
            src="{{ lifestyle_media.preview_image | image_url: width: 1500 }}"
            srcset="{{ lifestyle_media.preview_image | image_url: width: 720 }} 720w, {{ lifestyle_media.preview_image | image_url: width: 1100 }} 1100w, {{ lifestyle_media.preview_image | image_url: width: 1500 }} 1500w"
            sizes="(min-width: 900px) 54vw, 100vw"
            width="{{ lifestyle_media.preview_image.width }}"
            height="{{ lifestyle_media.preview_image.height }}"
            alt="{{ lifestyle_media.alt | default: 'The complete eight-pin collection arranged on a canvas bag' | escape }}"
            loading="lazy">
        {%- endif -%}
      </div>
      <div class="evo-pin-story__copy">
        <p class="evo-pin-eyebrow" data-evo-story-eyebrow>Wear the whole color story</p>
        <h2 id="evo-pin-story-one-{{ section.id }}" data-evo-story-title>Pick a spot. Keep the set together.</h2>
        <p data-evo-story-copy>A dark canvas bag turns the eight colors into one graphic little constellation. A jacket makes it louder. A board lets the full lineup sit still for once.</p>
        <ul>
          <li>Eight coordinated enamel pins</li>
          <li>Secure metal clutch-style backings</li>
          <li>Designed for fabric and pin displays</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="evo-pin-inhand" aria-labelledby="evo-pin-inhand-{{ section.id }}">
    <div class="evo-pin-shell evo-pin-inhand__grid">
      <div class="evo-pin-inhand__copy">
        <p class="evo-pin-eyebrow">The useful scale check</p>
        <h2 id="evo-pin-inhand-{{ section.id }}">Small enough to pin. Complete enough to display.</h2>
        <p>The in-hand view is the honest one: a compact eight-piece set with enough color and character to read as a collection, not a handful of random singles.</p>
        <div class="evo-pin-spec-row">
          <div><strong>8</strong><span>pins total</span></div>
          <div><strong>1</strong><span>complete option</span></div>
          <div><strong>$30</strong><span>for the set</span></div>
        </div>
      </div>
      <div class="evo-pin-inhand__image">
        {%- if hand_media != blank -%}
          <img
            src="{{ hand_media.preview_image | image_url: width: 1400 }}"
            srcset="{{ hand_media.preview_image | image_url: width: 720 }} 720w, {{ hand_media.preview_image | image_url: width: 1050 }} 1050w, {{ hand_media.preview_image | image_url: width: 1400 }} 1400w"
            sizes="(min-width: 900px) 48vw, 100vw"
            width="{{ hand_media.preview_image.width }}"
            height="{{ hand_media.preview_image.height }}"
            alt="{{ hand_media.alt | default: 'All eight enamel pins held together on a display card' | escape }}"
            loading="lazy">
        {%- endif -%}
        <span>All eight shown · All eight included</span>
      </div>
    </div>
  </section>

  <section class="evo-pin-details" aria-labelledby="evo-pin-details-{{ section.id }}">
    <div class="evo-pin-shell evo-pin-details__grid">
      <div>
        <p class="evo-pin-eyebrow">The short version</p>
        <h2 id="evo-pin-details-{{ section.id }}">Everything you need to know.</h2>
        <p>One set, one price, no sizing chart, no option maze.</p>
      </div>
      <div class="evo-pin-accordions">
        <details open>
          <summary>What arrives <span aria-hidden="true">+</span></summary>
          <div><p>One complete eight-piece enamel pin collection with metal clutch-style backings. The product gallery shows the full set included in your order.</p></div>
        </details>
        <details>
          <summary>Where to use them <span aria-hidden="true">+</span></summary>
          <div><p>Made for backpacks, jackets, hats, canvas bags, cork boards, shadow boxes, and pin display panels. Use the eight together or split them across a few surfaces.</p></div>
        </details>
        <details>
          <summary>Shipping <span aria-hidden="true">+</span></summary>
          <div><p>Standard U.S. delivery is estimated at 5–8 business days. Tracking is sent as soon as the set is on its way.</p></div>
        </details>
        <details>
          <summary>Care <span aria-hidden="true">+</span></summary>
          <div><p>Remove pins before washing a garment or bag. Wipe the enamel gently with a dry, soft cloth and keep the backings secured between wears.</p></div>
        </details>
      </div>
    </div>
  </section>

  <div class="evo-pin-sticky" data-evo-sticky aria-hidden="true">
    <div class="evo-pin-sticky__inner">
      <div class="evo-pin-sticky__meta">
        <strong class="evo-pin-sticky__price">{{ current_variant.price | money }}</strong>
        <span class="evo-pin-sticky__variant">8-Pin Collection</span>
      </div>
      <button
        class="btn evo-pin-sticky__button zenkai-sticky-atc__button"
        type="button"
        data-evo-sticky-add
        {% unless current_variant.available %}disabled{% endunless %}>
        {%- if current_variant.available -%}Add 8-Pin Collection{%- else -%}Sold out{%- endif -%}
      </button>
    </div>
  </div>

  <script type="application/json" data-evo-angle-config>
    {
      "completion": {
        "heroImage": {{ 'evolution-angle-completion-hero-45.webp' | asset_url | json }},
        "heroAlt": "The complete eight-pin collection arranged as a satisfying collector checklist",
        "heroEyebrow": "8 of 8 · complete",
        "heroTitle": "All eight. Nothing missing.",
        "heroLede": "No random assortment, no choosing one favorite, no half-finished lineup. The complete eight-pin collection arrives together for $30.",
        "giftLead": "Completion, instantly.",
        "giftCopy": "One order turns the whole color lineup into a finished collection.",
        "miniLead": "Exactly what is shown.",
        "miniCopy": "Eight distinct enamel pins, with no duplicate or mystery pull.",
        "manifestoEyebrow": "The satisfying full set",
        "manifestoTitle": "The little click of a collection becoming complete.",
        "manifestoCopy": "Every color, every tiny mood, and every design shown is already here. Open one package and the set is finished.",
        "proof": [
          ["8 / 8 from the start", "Every pin in the lineup arrives together in one order."],
          ["No mystery assortment", "The exact eight shown are the exact eight included."],
          ["One finished display", "Keep the rows together or spread the full set across your favorite places."]
        ],
        "storyEyebrow": "The complete color story",
        "storyTitle": "Eight designs make the display feel finished.",
        "storyCopy": "Together, the colors read like one deliberate collection. Split them up later if you want—the satisfying part is knowing none are missing."
      },
      "nostalgia": {
        "heroImage": {{ 'evolution-angle-nostalgia-hero-45.webp' | asset_url | json }},
        "heroAlt": "The complete eight-pin collection held beside a warm blue-hour collector desk",
        "heroEyebrow": "A little piece of the adventure",
        "heroTitle": "The set your younger self would have loved.",
        "heroLede": "Eight familiar little moods, now small enough for the bag, board, or shelf beside the setup you finally built.",
        "giftLead": "Grown-up shelf. Same soft spot.",
        "giftCopy": "A complete little lineup that makes an ordinary corner feel strangely familiar.",
        "miniLead": "The feeling is the point.",
        "miniCopy": "A colorful reminder of the adventures that never really left.",
        "manifestoEyebrow": "Tiny time machine",
        "manifestoTitle": "Some collections are really little ways back.",
        "manifestoCopy": "The colors, the different personalities, the urge to keep the whole crew together—some things still land exactly the way they used to.",
        "proof": [
          ["The whole crew", "All eight arrive together, so the memory feels complete from day one."],
          ["Made for now", "Glossy enamel and secure backings for the bag, jacket, board, or shelf you use today."],
          ["Easy to keep close", "One compact set with enough color to change the feeling of a familiar space."]
        ],
        "storyEyebrow": "Put the memory somewhere visible",
        "storyTitle": "A little nostalgia that gets to leave the shelf.",
        "storyCopy": "Pin the full crew to a daily bag, let one peek out from a jacket, or keep the eight together where you can see them."
      },
      "style": {
        "heroImage": {{ 'evolution-angle-style-hero-45.webp' | asset_url | json }},
        "heroAlt": "The complete eight-pin collection styling a charcoal canvas bag in an editorial fabric collage",
        "heroEyebrow": "Make the bag yours",
        "heroTitle": "Your bag is about to get a lot less boring.",
        "heroLede": "Eight bright enamel pins turn plain canvas, denim, or a display board into something unmistakably yours.",
        "giftLead": "One set. Endless placements.",
        "giftCopy": "Keep the color story together or move each little mood wherever it fits.",
        "miniLead": "Color does the work.",
        "miniCopy": "Black outlines and glossy enamel stay readable against denim, canvas, and dark fabric.",
        "manifestoEyebrow": "Small detail · big shift",
        "manifestoTitle": "A tiny detail can change the whole thing.",
        "manifestoCopy": "The bag stays useful. The jacket still goes with everything. Eight bright little pins just make the surface feel like it belongs to you.",
        "proof": [
          ["Instant transformation", "A plain canvas panel becomes a full color statement in a few minutes."],
          ["Eight styling pieces", "Cluster the set, balance the colors, or give each pin its own place."],
          ["Easy to rearrange", "Secure clutch-style backings let the layout change whenever your mood does."]
        ],
        "storyEyebrow": "Wear the whole color story",
        "storyTitle": "Start with the bag you already carry.",
        "storyCopy": "Dark canvas makes the eight colors pop. Denim softens them. A display board turns the full lineup into graphic wall art."
      }
    }
  </script>
</div>

<script src="{{ 'evolution-pin-set.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Evolution pin set",
  "settings": []
}
{% endschema %}
