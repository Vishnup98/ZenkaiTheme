<button class="ec-cta" type="submit" {% if form_id != blank %}form="{{ form_id }}"{% endif %} {% if main %}name="add" data-ec-main-cta{% else %}data-ec-inline-cta{% endif %} {% unless ready %}disabled{% endunless %}>{% if ready %}<span>Make all 9 mine{% if show_price %} <span aria-hidden="true">—</span> {{ variant.price | money }}{% endif %}</span><svg class="mc-cta-arrow" viewBox="0 0 24 24" fill="none" aria-hidden="true" focusable="false"><path d="M5 19 19 5M6 5h13v13" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>{% elsif launch_ready %}Sold out{% else %}Coming soon{% endif %}</button>
