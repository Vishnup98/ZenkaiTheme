{%- assign zenkai_is_product_page = false -%}
{% if template contains 'product' %}
  {%- assign zenkai_is_product_page = true -%}
{% endif %}

<div class="announcement" style="background: #1a1b2e;">
  <div class="announcement__wrapper">
    {% if section.settings.announcement_link != blank %}
      <a href="{{ section.settings.announcement_link }}" class="announcement__link">
    {% endif %}

    <div class="announcement__text{% unless zenkai_is_product_page %} announcement__text--rotating{% else %} announcement__text--static{% endunless %}"
         {% unless zenkai_is_product_page %}data-rotating-messages='["🚚 FREE SHIPPING. Always.", "Premium Quality Materials", "Made to Order in 2-5 Days"]'{% endunless %}>
      {{ section.settings.announcement_text | strip_html }}
    </div>

    {% if section.settings.announcement_link != blank %}
      </a>
    {% endif %}
  </div>
</div>

<script>
(function() {
  const textEl = document.querySelector('.announcement__text--rotating');
  if (!textEl) return;

  const messages = JSON.parse(textEl.dataset.rotatingMessages || '[]');
  if (messages.length < 2) return;

  let currentIndex = 0;

  function rotateMessage() {
    textEl.style.opacity = '0';

    setTimeout(() => {
      currentIndex = (currentIndex + 1) % messages.length;
      textEl.textContent = messages[currentIndex];
      textEl.style.opacity = '1';
    }, 300);
  }

  setInterval(rotateMessage, 4000);
})();
</script>

<style>
.announcement__text--rotating {
  transition: opacity 0.3s ease;
}
/* Night Signal - Midnight Indigo Announcement Bar */
.announcement {
  background: #1a1b2e !important;
}
.announcement__text,
.announcement__link,
.announcement__text--rotating {
  color: #ffffff !important;
}
{% if template contains 'product' %}
  .announcement__wrapper {
    min-height: 30px;
  }

  .announcement__text,
  .announcement__link,
  .announcement__text--rotating,
  .announcement__text--static {
    font-size: 10px;
    letter-spacing: 0.16em;
    line-height: 1.2;
    padding-top: 6px;
    padding-bottom: 6px;
  }
{% endif %}
</style>
